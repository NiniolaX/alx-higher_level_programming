-- Script computes the average of all records in a column of a specified table
-- Compute score average of all records in table second_table of database hbtn_0c_0
SELECT AVG(score) AS average
	FROM second_table;
